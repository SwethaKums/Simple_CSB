    <!DOCTYPE html>
<html>
  <head>
    <title>Simple csb: <%= " | #{@page_title}" if @page_title %></title>
    <%= stylesheet_link_tag("application",:media=>'all') %>
  </head>
  
  <body>

  <% if !flash[:notice].blank? %>
    <div class="notice">
      <%= flash[:notice] %>
    </div>
  <% end %>
  

  <!-- before yield -->
  <%= yield %>
  <!-- after yield -->

  </body>
</html>